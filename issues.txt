It works for the most part.

Now we need to find a way to have it provide different colors if there is a variable or something inside of the Alias Function ()'s.

We also need it to have it override the Custom Dot Command green coloring with red if the Dot Command is Native.